{
  "name": "back-end",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start-sub": "node dist/subscriber.js",
    "start-api": "node dist/reader.js",
    "sub": "npm run build && npm run start-sub",
    "api": "npm run build && npm run start-api"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.13.2",
    "mqtt": "^5.10.4"
  }
}
